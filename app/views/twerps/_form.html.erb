<%= form_with(model: twerp) do |form| %>
  <% if twerp.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(twerp.errors.count, "error") %> prohibited this twerp from being saved:</h2>

      <ul>
        <% twerp.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :name, style: "display: block" %>
    <%= form.text_field :name %>
  </div>

  <div>
    <%= form.label :content, style: "display: block" %>
    <%= form.text_area :content %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
